#include <stdio.h>
#include <stdlib.h>

int main()
{

    char first_name = 'P';
    char last_name = 'F';
    (last_name > first_name) ? printf("last_name") :
                               printf("first_name");
    return 0;
}
